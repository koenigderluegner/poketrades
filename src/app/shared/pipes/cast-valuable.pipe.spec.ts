import { CastValuablePipe } from './cast-valuable.pipe';

describe('CastValuablePipe', () => {
  it('create an instance', () => {
    const pipe = new CastValuablePipe();
    expect(pipe).toBeTruthy();
  });
});

import { CastBreedablePipe } from './cast-breedable.pipe';

describe('CastBreedablePipe', () => {
  it('create an instance', () => {
    const pipe = new CastBreedablePipe();
    expect(pipe).toBeTruthy();
  });
});

import { FilterLevelUpMovesPipe } from './filter-level-up-moves.pipe';

describe('FilterLevelUpMovesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLevelUpMovesPipe();
    expect(pipe).toBeTruthy();
  });
});

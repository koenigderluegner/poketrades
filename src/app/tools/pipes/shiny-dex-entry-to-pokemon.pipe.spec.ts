import { ShinyDexEntryToPokemonPipe } from './shiny-dex-entry-to-pokemon.pipe';

describe('ShinyDexEntryToPokemonPipe', () => {
  it('create an instance', () => {
    const pipe = new ShinyDexEntryToPokemonPipe();
    expect(pipe).toBeTruthy();
  });
});
